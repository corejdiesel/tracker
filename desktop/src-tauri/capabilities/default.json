{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Tauri v2 denies every command by default — this is what actually allows the frontend to reach db_query/db_execute/db_execute_batch and the keychain_* commands in commands.rs.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
